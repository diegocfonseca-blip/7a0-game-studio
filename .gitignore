# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/settings.local.json

# segredos locais (nunca commitar)
.env

# 🖼️ Mockups gerados pelos scripts (mockup-batismo, mockup-lendas, campinho,
# padrão visual, folhas de rosto…). O SCRIPT mora no repo — a imagem não, porque
# ela é refeita com um comando e são centenas de KB cada. Pra mandar pro Diego,
# roda o script e manda o arquivo; pra guardar de vez, usa --saida num caminho
# fora da raiz do repo.
/mockup-batismo.png
/lendas.png
/campinho.png
/padrao-visual.png
/post-regra.png
/compara.png
/rostos.png
/rostos2.png
/rostos3.png

/liga-fechada.png

# 🔬 scripts de teste descartáveis (ficam DENTRO do repo só pra achar o
# node_modules; não são entrega e não devem ser versionados)
scripts/_tmp-*.mjs
bench-sim.html

# 📣 artes de DIVULGAÇÃO geradas por script (a fonte é scripts/post-divulgacao.mjs;
# a imagem sai dela em 2 segundos, não precisa versionar PNG de 1 MB)
/divulgacao-feed.png
/divulgacao-stories.png
