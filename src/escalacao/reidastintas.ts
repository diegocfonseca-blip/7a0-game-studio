// Logo Rei das Tintas (patrocinador do campinho) — PNG embutido como data URI
// pra funcionar em todo lugar (inclusive no print de compartilhar).
// 🎨 Rei das Tintas — logo de patrocínio. Era uma imagem em base64 colada AQUI (todo
// jogador baixava no bundle, mesmo sem abrir o estádio). Virou arquivo .webp em
// img/, igual aos batismos: só desce pra quem vê a tela. O export não mudou de
// nome de propósito — quem usa (estadio.tsx/screens.tsx) não precisou mexer.
import logo from './img/patro-reidastintas.webp'
export const REIDASTINTAS_LOGO = logo
